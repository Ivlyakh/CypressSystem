version: 0.2
phases:
  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...
      - docker build --tag Cypress:latest  .
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Running the Docker image...
      - docker run Cypress:latest