version: 0.2
phases:
  pre_build:
    commands:
      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
  build:
    commands:
      - echo Building the Docker image...
      - docker build --tag $COMMIT_HASH:latest  .
  post_build:
    commands:
      - echo Running the Docker image...
      - export HTTP_PROXY="http://lum-customer-hl_3e04d11e-zone-static-country-gb:wobx3fw1htcl@zproxy.lum-superproxy.io:22225"
      - export HTTPS_PROXY="http://lum-customer-hl_3e04d11e-zone-static-country-gb:wobx3fw1htcl@zproxy.lum-superproxy.io:22225"
      - docker run $COMMIT_HASH:latest